#include <math.h>
#include <stdio.h>
int main()
{
  double Number;
  Number = 64;
  double squareRoot = sqrt(Number);
  
  printf("The Square root of %.2lf = %.2lf", 
          Number, squareRoot);
  return 0;
}

OUTPUT :

The Square root of 64.00 = 8.00
