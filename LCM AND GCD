#include<stdio.h>
int main()
{
int a,b,lcm,gcd,i;
printf("Enter the values of a and b");
scanf("%d%d",&a,&b);
for(i=1;i<=a&&i<=b;i++)
{
if(a%i==0&&b%i==0)
{
gcd=i;
}
}
lcm=(a*b)/gcd;
printf("gcd=%d\n",gcd);
printf("lcm=%d",lcm);
return 0;
}


OUTPUT:
Enter the values of a and b2 3
gcd=1
lcm=6
